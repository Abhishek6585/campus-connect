# Dependencies
node_modules/
**/node_modules/

# Environment secrets — never commit these
.env
.env.local
*.env
!.env.example

# Old JSON-file database (no longer used now that Postgres is wired up,
# but ignored anyway in case it's regenerated locally)
backend/src/data/db.json

# Vercel
.vercel

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Build artifacts
dist/
build/
