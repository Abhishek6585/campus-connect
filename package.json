{
  "name": "campus-connect-student-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Student portal monorepo: api/ (Vercel serverless function wrapping the Express backend), frontend/ (student app), admin/ (admin panel), backend/ (Express source + Postgres data layer + seed script).",
  "scripts": {
    "seed": "cd backend && npm install && npm run seed",
    "dev": "cd backend && npm install && npm start"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "pg": "^8.22.0"
  }
}
